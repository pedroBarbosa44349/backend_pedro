// Template (modelo) string ou template literals
// São texte que processam informações 
// de natureza distinta numa sentença
// Ex. `Minhaa mãe é ${nomeDaMae}`
// Observe a estrutura padronizada com `${}`

const nome = 'Pedro'
const sobrenome = 'Henrique'
const perfil = ` \nOlá, ${nome} - \n${sobrenome}`
console.log(perfil)
